apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: noderegistry
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
    scrape_configs:
      - job_name: 'api'
        static_configs:
          - targets: ['api:8080']
